import LegalLensStartup from "./components/LegalLensStartup";

/**
 * Prototype root. The whole experience is the startup sequence that
 * resolves into the login screen — there is no other application here.
 */
export default function App() {
  return <LegalLensStartup />;
}