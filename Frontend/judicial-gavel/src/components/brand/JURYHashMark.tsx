/** Jury Hash insignia: a judicial eye formed from scale beams and an iris. */
export function JuryHashMark({ className }: { className?: string }) {
  return (
    <svg
      className={className}
      viewBox="0 0 64 64"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      aria-label="Jury Hash insignia"
      role="img"
    >
      {/* Keep the existing SVG contents from JuryHashMark.tsx here */}
    </svg>
  );
}
